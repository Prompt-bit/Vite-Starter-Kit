// Legacy entry (kept for compatibility).
// Use `src/main.js` as the main entry point.
// This file is intentionally minimal to avoid duplication.
console.warn('Legacy index.js — entry moved to src/main.js');
